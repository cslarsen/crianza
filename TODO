vm: allow floats
vm: fixnum arithmetic (as option)
vm: cannot parse negative numbers, e.g. "-123" is parsed as "- 123"
genetic: crossover subregions, not from start to end
genetic: higher score to programs that terminate within steps
